<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple note making app</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input name="title" class="block w-full text-white rounded-md px-5 py-2 bg-zinc-800" type="text" placeholder="Title goes">
                <textarea name="details" class="block mt-2 w-full text-white px-5 py-2 resize-none bg-zinc-800" placeholder="Write your task details.."></textarea>
                <input class="mt-2 text-white bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task">

            </form>
        </div>
        <div class="task flex gap-3 flex-wrap p-10">
            <% if(files.length>0){ %>
              <% files.forEach(function(val) {%>
                <div class="task min-w-72  px-3 rounded-md py-4 bg-zinc-800">
                    <h1 class="text-white text-3xl tracking-tighter"><%=val %></h1>
                    <a class="text-blue-500 inline-block mt-3" href="/file/<%= val %>">Read more.</a>
                </div>
           <% }) %>
            <%} else {%>
                <h3 class="text-zinc-600">No task yet..</h3>
                <%}%>
        </div>

    </div>
</body>
</html>